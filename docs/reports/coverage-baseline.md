# 覆盖率基线 — PageWise

> **文档编号**: coverage-baseline
> **创建日期**: 2026-05-21
> **迭代**: R233 (CoverageGateHardening)
> **状态**: 锁定 — 任何阈值变更必须同步更新本文档

---

## 1. 基线快照（2026-05-21 实测）

| 指标 | 分子 (覆盖) | 分母 (总计) | 覆盖率 | 门禁阈值 | 容差下限 (基线 − 2pp) |
|------|------------|------------|--------|---------|---------------------|
| **Lines** | 12,048 | 50,872 | **23.68%** | ≥ 23% | 21.68% |
| **Statements** | 12,048 | 50,872 | **23.68%** | (同 Lines) | 21.68% |
| **Branches** | 1,970 | 2,593 | **75.97%** | ≥ 75% | 73.97% |
| **Functions** | 449 | 919 | **48.85%** | ≥ 48% | 46.85% |

> ⚠️ 以上数据为 `npm run test:coverage` 实测结果，非声称值。门禁阈值精确到整数位，向下取整以避免因浮点精度导致假阳性。

---

## 2. 测量环境

| 项目 | 值 |
|------|---|
| **日期** | 2026-05-21 |
| **OS** | Ubuntu 22.04 (GitHub Actions ubuntu-latest) |
| **Node.js** | v22.22.2 |
| **c8 版本** | ^10.1.3 |
| **测试命令** | `npm run test:ci:coverage` (node --test --test-concurrency=1) |
| **覆盖率工具** | c8 (V8 native coverage, 零插桩) |
| **报告格式** | lcov + text-summary + json-summary |
| **测试用例数** | 7484 (pass 7470, fail 14) |

---

## 3. 门禁阈值映射

| 维度 | 基线值 | 门禁阈值 | 回归容差 | 退化触发阈值 |
|------|--------|---------|---------|-------------|
| Lines | 23.68% | 23% | 2pp | < 21.68% |
| Branches | 75.97% | 75% | 2pp | < 73.97% |
| Functions | 48.85% | 48% | 2pp | < 46.85% |

**双层门禁机制**:
1. **绝对阈值** (`npm run coverage:gate`): 任一维度低于门禁阈值 → CI fail
2. **回归检测** (`scripts/architecture-guard.sh`): 任一维度低于基线值 − 2pp → CI fail

---

## 4. 历史声称 vs 实测对比

| 迭代 | 声称行覆盖率 | 实测行覆盖率 | 声称函数覆盖率 | 实测函数覆盖率 | 门禁阈值 | 真实通过？ |
|------|------------|------------|-------------|-------------|---------|-----------|
| R205 | ≥50% | 23.72% | ≥60% | ~48% | `--lines 20` | ⚠️ 门禁通过但未达声称值 |
| R216 | ≥40% | 23.22% | ≥55% | ~48% | `--lines 35`（声称） | ❌ 声称阈值未生效 |
| R222 | ≥50% | ~23% | ≥60% | ~48% | 未验证 | ❌ 未验证 |
| R225 | ≥50% | ~23% | ≥60% | ~48% | 未验证 | ❌ 未验证 |
| R230 | ≥50% | 23.68% | ≥60% | 48.85% | `--lines 50 --functions 60` | ❌ 死锁：阈值高于实测 |
| **R233** | — | **23.68%** | — | **48.85%** | `--lines 23 --functions 48 --branches 75` | ✅ 基线锁定，门禁对齐 |

---

## 5. 更新规则

1. **收紧阈值**: 当覆盖率经实测确认稳定高于当前基线 ≥2pp 时，可向上修正门禁阈值，同时更新本文档基线快照
2. **修改容差**: 需经代码审查确认，容差不得 >5pp
3. **基线刷新**: 每次运行 `npm run test:coverage` 后，对比本文档中的基线值，如有显著变化（≥1pp）则更新快照
4. **谁有权修改**: 任何开发者可通过 PR 修改，但必须附带实测覆盖率输出作为证据

---

## 6. 关联文件

| 文件 | 用途 |
|------|------|
| `package.json` → `coverage:gate` | 绝对阈值门禁 |
| `scripts/architecture-guard.sh` | 回归检测脚本 |
| `.github/workflows/ci.yml` | CI 流水线调用 |
| `coverage/coverage-summary.json` | c8 生成的当前覆盖率数据 |
| `docs/DESIGN-ITER5.md` | R233 设计文档 |

---

*基线文档创建于 2026-05-21 by R233 CoverageGateHardening*
